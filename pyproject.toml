[project]
name = "tadawul-data-platform"
version = "0.1.0"
description = "Data engineering and analytics platform built from Saudi Exchange data"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "pandas",
    "python-dotenv",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]